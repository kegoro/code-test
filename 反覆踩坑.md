# 反覆踩坑記錄

> 這份專門記「同一個坑踩過很多次」的問題。
> 目的不是再記一筆交差，是讓同樣的錯**真的不要再發生**。
> 每條都寫清楚：你會看到什麼症狀、真正的根因、為什麼會一再發生、唯一正解、**你自己（不寫程式）怎麼處理**。

---

## 1. smc_bot 一直啟動不了（2026-06-15 終於查到真因）

### 你會看到的症狀
- 雙擊啟動或開機後，Telegram bot 沒反應，`/fin` 之類指令沒人回。
- 螢幕上看不到任何錯誤訊息（因為它是「無視窗」在背景跑）。
- 過去每次對話都說「修好了」，結果下次又壞 —— 這就是你受不了的點。

### 真正的根因（這次徹底查清楚，不是又猜）
啟動檔 `start_smc_bot.bat` 和 `launch_smc_bot.vbs` 裡面寫了**中文**（註解、說明文字），而且檔案是用 **UTF-8** 格式存的。
但 Windows 的命令列（cmd.exe）在雙擊執行時，預設是用 **Big5（CP950）** 去讀檔。

→ 用 Big5 去讀 UTF-8 的中文 = 一整排亂碼，連「換行」都被亂碼吃掉
→ cmd 還沒跑到「啟動 python」那一行，就已經被亂碼弄到解析中斷
→ bot 根本沒被啟動。又因為它無視窗，你什麼錯誤都看不到，只看到「怎麼又沒起來」。

**一句話記住：啟動檔裡有中文 → Windows 讀成亂碼 → 程式還沒啟動就掛了。**

### 為什麼會一再發生（這才是重點）
1. 那兩個啟動檔一直放著中文，沒人意識到「中文」本身就是元兇。
2. 它無視窗執行，壞了看不到錯誤 → 很難診斷 → 每次都當成別的問題。
3. 過去每次對話，都是「臨時用別的方法硬把它撐起來」，**沒去修那個壞掉的檔，也沒把能動的版本固定下來** → 下次你又去雙擊那個壞檔 → 同一個坑再踩一次。

### 唯一正解（已實測，2026-06-15）
**啟動檔（.bat / .vbs）永遠只能用英文（ASCII），一個中文都不能放。**
- 已把 `start_smc_bot.bat`、`launch_smc_bot.vbs` 裡的中文全部改成英文。
- 已實測：走「雙擊 vbs → bat → python」這條你平常會用的路徑，bot 成功啟動（這條路以前永遠失敗）。
- bat 裡內建「crash 後 5 秒自動重啟」的迴圈，所以現在 **bot 掛掉、網路抖一下、重開機**，都會自動把它拉回來。
- 開機自啟也設好了（Windows 啟動資料夾裡放著 `launch_smc_bot.vbs`）。

### 你自己怎麼處理（不用寫程式）
- **最快確認 bot 活著**：直接在 Telegram 打 `/fin 2330`。有回 = bot 活著。
- **想手動重啟**：到桌面 `code test` 資料夾，雙擊 `launch_smc_bot.vbs`。等 20 秒，再去 Telegram 打 `/fin 2330` 確認。
- **想看是不是真的在跑**：Ctrl+Shift+Esc 開工作管理員 →「詳細資料」分頁 → 找 `python.exe`。應該有兩個（一個台股 daemon、一個 smc_bot）。
- **鐵則（請記住，也提醒我）**：以後不管誰要改這兩個啟動檔，**裡面永遠不可以出現中文**。要寫說明，就寫英文，或寫到這份 `反覆踩坑.md` 裡。

---

## 2. 重啟 bot 時陷入 Conflict 無限重啟（2026-06-16）

### 你會看到的症狀
- 改完程式想重啟 bot，結果 Telegram 一直沒回應。
- `_smc_bot.watchdog.log` 裡一整排紅字 `telegram.error.Conflict: terminated by other getUpdates request`，重複幾十次。

### 真正的根因
watchdog（`start_smc_bot.bat`）設計成「bot 一掛掉，**5 秒**後自動重啟」。
但 Telegram 那邊，一個 bot 連線被切斷後，**鎖要約 50 秒才會釋放**。
→ 舊的 bot 還沒被 Telegram 放掉，新的 5 秒就搶著上線 → 撞 `Conflict` → 崩 → 5 秒又起 → 又撞 → **無限 crash-loop**。
特別容易發生在「只殺掉 python、沒殺 watchdog」的時候：你殺了 python，watchdog 5 秒內又生一個，剛好撞上舊連線。

### 唯一正解（已實測 2026-06-16）
重啟要「**全部殺乾淨 → 等一下 → 只啟動一個**」：
1. 先殺 **watchdog 的 cmd 視窗**（父程序，不然它會一直幫你重生 python）。
2. 再殺 **python**（smc_bot 本體）。
3. 確認兩個都死光，**靜置一下**讓 Telegram 把鎖放掉。
4. 雙擊 `launch_smc_bot.vbs` 單一啟動。
5. 確認 log 出現**新的**一行 `Application started`（時間是現在），且沒有再冒 `Conflict`。

### 你自己怎麼處理（不用寫程式）
- 你平常不會遇到（只有「改完程式要重啟」才會），這條主要是提醒我。
- 如果你自己手癢重開：**先到工作管理員把 `python.exe`（smc_bot 那個）和它的黑色 cmd 視窗都結束**，等個半分鐘，再雙擊 `launch_smc_bot.vbs`。不要連點兩次。
