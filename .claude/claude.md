## 對話啟動規則（最優先）
- **每次新對話開始前**，先 Read 專案根目錄的 `LESSONS.md`
- 該檔案包含跨對話的策略決策、踩坑紀錄、待驗證事項
- 完成回測 / 改策略 / 發現新坑後，**主動**詢問是否要更新 `LESSONS.md`

## 回覆規則
- 只輸出需要修改的程式碼片段
- 不要前言、不要解釋、不要摘要
- 有多個修改點時用 `# 檔案名:函數名` 標示位置